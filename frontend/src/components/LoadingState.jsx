export default function LoadingState({ label = "Loading data" }) {
  return <div className="rounded-lg border border-grid-line bg-white p-8 text-center text-sm font-medium text-slate-500">{label}...</div>;
}
